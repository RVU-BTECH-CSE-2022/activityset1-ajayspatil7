#include<stdio.h>

int main(){

  float length;
  float breadth;  
  float area = length*breadth;
  printf("The area of rectangle with length %f and breadth %f is %f\n", length, breadth, area);
  return 0;  
}
