#include<stdio.h>

int main(void){
  float length, breadth, area;
  length = 10;
  breadth = 20;
  area = length * breadth;
  printf("The area of Rect with %f as breadth and %f as length is %f \n", length, breadth, area);
  return 0;

  
}
